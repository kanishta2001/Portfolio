# Portfolio

This is my Porthfolio. The project uses `Next.js App Router`, `React`, `TypeScript`, `Tailwind CSS`, `Motion`, and `Lucide React`.

## Running the project

```bash
npm install
npm run dev
```

Then open `http://localhost:3000` in your browser.

For a production check:

```bash
npm run lint
npm run build
```

